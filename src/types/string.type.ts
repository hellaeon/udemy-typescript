namespace StringType {
  // string, double or single quotes
  let stringValue: string = "dfsdfdfs";
  stringValue = 'sdfsdf';

  // expression
  stringValue = "sdfsdf" + " yep, this works";
  stringValue = stringValue + " yep, this works";
}